<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="searchVm"
            type="com.example.kakao.uilayer.ui.search.SearchResultViewModel" />
        <variable
            name="imageAdt"
            type="com.example.kakao.uilayer.adapter.ImageAdapter" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="@dimen/contentHorizontalPadding"
        android:paddingEnd="@dimen/contentHorizontalPadding"
        errorMessageFromAdt="@{imageAdt.loadStateFlow}"
        errorMessageFromVm="@{searchVm.errorMessage}"
        tools:context=".uilayer.ui.search.SearchResultFragment">

        <EditText
            android:id="@+id/etSearch"
            android:layout_width="0dp"
            android:layout_height="@dimen/searchHeight"
            android:padding="@dimen/searchPadding"
            android:hint="@string/searchHint"
            android:maxLines="1"
            android:imeOptions="actionDone"
            app:layout_constraintHorizontal_weight="7"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btnSearch"
            style="@style/CustomEditTextStyle" />

        <Button
            android:id="@+id/btnSearch"
            android:layout_width="0dp"
            android:layout_height="@dimen/searchHeight"
            android:text="@string/search"
            android:gravity="center"
            app:layout_constraintHorizontal_weight="3"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/etSearch"
            app:layout_constraintEnd_toEndOf="parent"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvSearchResult"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintTop_toBottomOf="@id/etSearch"
            app:layout_constraintBottom_toBottomOf="parent"
            submitImageData="@{searchVm.homeItemUiState}"
            modifyItem="@{searchVm.modifyingUiState}"
            tools:listitem="@layout/item_image"/>

        <ProgressBar
            android:id="@+id/loadingBar"
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            loadingStateFromAdt="@{imageAdt.loadStateFlow}"
            loadingStateFromVm="@{searchVm.isLoading}"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
